use proconio::input;
use rustc_hash::FxHashMap;

fn main() {
    input! {
        n: usize,
        k: usize,
        mut ps: [u32;n],
    };
    let tab = vec![];
    p.reverse();
    for t in 0..n {
        if let Some(p) = ps.pop() {}
    }
}
